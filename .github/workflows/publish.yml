# ── Ipp Language — CI/CD Pipeline ────────────────────────────────────────────
# Publishes to PyPI automatically when VERSION in ipp/main.py changes on main.
# Also runs regression tests on every push and PR.

name: CI / Publish to PyPI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:    # Allow manual trigger

jobs:
  # ── Job 1: Tests ─────────────────────────────────────────────────────────────
  test:
    name: Regression Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package (editable)
        run: pip install -e . --break-system-packages || pip install -e .
        # Some tests need optional deps:
      - name: Install optional test dependencies
        run: pip install pyyaml --break-system-packages || pip install pyyaml

      - name: Run regression tests
        run: |
          python -B run_tests.py 2>&1 | tee /tmp/regression_out.txt
          # Exit non-zero if any [FAIL] lines appear
          FAILED=$(grep "^\[FAIL\]" /tmp/regression_out.txt || true)
          if [ -n "$FAILED" ]; then
            echo "::error::Test failures detected:"
            echo "$FAILED"
            exit 1
          fi

  # ── Job 2: Extract version ────────────────────────────────────────────────────
  check-version:
    name: Detect version
    runs-on: ubuntu-latest
    outputs:
      version:    ${{ steps.ver.outputs.version }}
      tag-exists: ${{ steps.tag.outputs.exists }}
    steps:
      - uses: actions/checkout@v4
        # No fetch-depth needed — we don't check tags, we check PyPI

      - name: Extract version from ipp/main.py
        id: ver
        run: |
          VERSION=$(python3 -c "
          import re, pathlib
          src = pathlib.Path('ipp/main.py').read_text()
          m = re.search(r'VERSION\s*=\s*[\"\']([\w.]+)[\"\']\s*$', src, re.M)
          print(m.group(1) if m else 'unknown')
          ")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

      - name: Check if version already published on PyPI
        id: tag
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://pypi.org/pypi/ipp-lang/$VERSION/json")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Version $VERSION already published on PyPI — skipping publish"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Version $VERSION is new — will publish"
          fi

  # ── Job 3: Publish ────────────────────────────────────────────────────────────
  publish:
    name: Build & Publish to PyPI
    runs-on: ubuntu-latest
    needs: [test, check-version]
    # Publish on push to main (including manual workflow_dispatch re-runs),
    # but only when the version hasn't already been published to PyPI
    if: |
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
      github.ref == 'refs/heads/main' &&
      needs.check-version.outputs.tag-exists == 'false'
    environment:
      name: pypi
      url: https://pypi.org/project/ipp-lang/${{ needs.check-version.outputs.version }}/
    permissions:
      contents: write   # needed to push git tag
      id-token: write   # needed for PyPI trusted publishing

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Set version env
        run: echo "VERSION=${{ needs.check-version.outputs.version }}" >> $GITHUB_ENV

      - name: Build package
        run: python -m build

      - name: Check distribution
        run: twine check dist/*

      - name: Create & push git tag v${{ env.VERSION }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          TAG="v${{ env.VERSION }}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists locally — skipping creation"
          else
            git tag -a "$TAG" -m "Release $TAG — auto-tagged by CI"
            git push origin "$TAG"
            echo "Pushed tag $TAG"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        continue-on-error: true   # safe if release already exists from a prior run
        with:
          tag_name: v${{ env.VERSION }}
          name: "Ipp v${{ env.VERSION }}"
          body: |
            ## Ipp Language v${{ env.VERSION }}

            Auto-published from `main` branch after passing all regression tests.

            ### Install
            ```bash
            pip install ipp-lang==${{ env.VERSION }}
            ```

            ### Run
            ```bash
            ipp run script.ipp      # interpreter mode
            ipp run --vm script.ipp # bytecode VM mode
            ipp repl                # interactive REPL
            ```

            ### Changelog
            See [ROADMAP_V2.md](https://github.com/authorss81/Ipp/blob/main/ROADMAP_V2%20(1).md)
          draft: false
          prerelease: ${{ contains(env.VERSION, 'alpha') || contains(env.VERSION, 'beta') || contains(env.VERSION, 'rc') }}
          files: dist/*

      - name: Publish to PyPI (Trusted Publishing)
        uses: pypa/gh-action-pypi-publish@release/v1
        # No PYPI_API_TOKEN needed — uses OIDC trusted publishing
        # Set up at: https://pypi.org/manage/project/ipp-lang/settings/publishing/

      # Fallback: token-based publish (comment out if using trusted publishing)
      # - name: Publish to PyPI (token fallback)
      #   env:
      #     TWINE_USERNAME: __token__
      #     TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      #   run: twine upload dist/*
